
for (let i = 0; i < 16; i++) {
    setTimeout(() => {
        const id = i;
        while (1) {
            console.log(id);
            const start = new Date
            while (new Date().getTime() < start.getTime()+1000) {
                
            }
        }
    })
}